<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dtstack.engine.dtscript.service.db.mapper.RdosNodeMachineMapper">
	
	<resultMap id="rdosNodeMachineResult" type="com.dtstack.engine.dtscript.service.db.dataobject.RdosNodeMachine">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP" />
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP" />
        <result column="ip" property="ip" jdbcType="VARCHAR" />
        <result column="port" property="port" jdbcType="BIGINT" />
        <result column="machine_type" property="machineType" jdbcType="INTEGER" />
        <result column="app_type" property="appType" jdbcType="VARCHAR" />
        <result column="is_deleted" property="isDeleted" jdbcType="INTEGER" />
        <result column="deploy_info" property="deployInfo" jdbcType="VARCHAR" />
    </resultMap>
	
    <insert id="insert" parameterType="com.dtstack.engine.dtscript.service.db.dataobject.RdosNodeMachine">
	replace into rdos_node_machine(gmt_create,gmt_modified,ip,port,machine_type,app_type,deploy_info)
	values(now(),now(),#{ip},#{port},#{machineType},#{appType}, #{deployInfo})
	<selectKey resultType="long" keyProperty="id" order="AFTER">
	   SELECT LAST_INSERT_ID() AS ID
	</selectKey>
	</insert>
	
   <update id="updateMachineType" parameterType="com.dtstack.engine.dtscript.service.db.dataobject.RdosNodeMachine">
        update rdos_node_machine set machine_type=#{machineType}, gmt_modified=now() where ip=#{ip} and port=#{port} and app_type=#{appType};
   </update>
   
   
  <update id="disableMachineNode" parameterType="com.dtstack.engine.dtscript.service.db.dataobject.RdosNodeMachine">
    update rdos_node_machine set is_deleted = 1, gmt_modified=now() where ip=#{ip} and port=#{port} and app_type=#{appType};
  </update>

    <update id="ableMachineNode" parameterType="com.dtstack.engine.dtscript.service.db.dataobject.RdosNodeMachine">
        update rdos_node_machine set is_deleted = 0, gmt_modified=now() where ip=#{ip} and port=#{port} and app_type=#{appType};
    </update>
    
    <update id="updateOneTypeMachineToSlave" parameterType="java.util.HashMap">
         update rdos_node_machine set machine_type=1, gmt_modified=now() where app_type=#{type};
    </update>

    <update id="updateMachineToMaster">
         update rdos_node_machine set machine_type=0, gmt_modified=now() where ip=#{ip} and app_type=#{appType};
    </update>

    <select id="listByAppType" resultMap="rdosNodeMachineResult">
        SELECT * FROM rdos_node_machine
        WHERE is_deleted=0 AND app_type=#{appType} ORDER BY machine_type ASC
    </select>

    <select id="getByAppTypeAndMachineType" resultMap="rdosNodeMachineResult">
        SELECT * FROM rdos_node_machine
        WHERE is_deleted=0 AND app_type=#{appType} AND machine_type=#{machineType} limit 1
    </select>
</mapper>
