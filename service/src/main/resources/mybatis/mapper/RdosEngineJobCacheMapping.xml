<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dtstack.rdos.engine.db.mapper.RdosEngineJobCacheMapper">
	
	<resultMap id="rdosEngineJobCacheResult" type="com.dtstack.rdos.engine.db.dataobject.RdosEngineJobCache">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP" />
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP" />
        <result column="is_deleted" property="isDeleted" jdbcType="INTEGER" />
        <result column="job_id" property="jobId" jdbcType="VARCHAR" />
        <result column="job_info" property="jobInfo" jdbcType="VARCHAR" />
        <result column="engine_type" property="engineType" jdbcType="VARCHAR"/>
        <result column="compute_type" property="computeType" jdbcType="INTEGER"/>
        <result column="stage" property="stage" jdbcType="INTEGER"/>
    </resultMap>

    <insert id="insert">
        replace into rdos_engine_job_cache( job_id, engine_type, compute_type, stage, job_info)
        values(#{jobId}, #{engineType}, #{computeType}, #{stage}, #{jobInfo});
    </insert>

    <delete id="delete">
        delete from rdos_engine_job_cache where job_id=#{jobId}
    </delete>

    <select id="getOne" resultMap="rdosEngineJobCacheResult">
        select * from rdos_engine_job_cache where job_id=#{jobId} and is_deleted=0 order by gmt_create desc limit 1;
    </select>

    <update id="updateStage">
        update rdos_engine_job_cache set stage = #{stage} where job_id = #{jobId}
    </update>

    <select id="listByStage" resultMap="rdosEngineJobCacheResult">
        select * from rdos_engine_job_cache where stage = #{stage} and is_deleted=0 order by gmt_create asc;
    </select>

</mapper>
