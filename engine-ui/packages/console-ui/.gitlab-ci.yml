before_script:
  - "npm -v"
  - "npm install --unsafe-perm=true --allow-root"

stages:
  - lintjs
  - lintdoc
  - test

lint-js: 
  stage: lintjs
  script: "npm run lint-js"

test:
  stage: test
  script: "npm test"

lint-doc:
  stage: lintdoc
  script: "npm run lint-doc"

