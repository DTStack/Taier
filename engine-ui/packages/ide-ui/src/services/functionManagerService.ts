import { FolderTreeService } from 'molecule/esm/services';

class FunctionManagerService extends FolderTreeService {
    constructor() {
        super();
    }
}

const functionManagerService = new FunctionManagerService();

export default functionManagerService;
